name: Release

# Releases are always manual — Actions → Release → Run workflow.
# One action = version + tag + GitHub release + VPS deploy.
# All policy lives in the shared workflows (see docs/git-rules.md).

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/family-release.yml
    permissions:
      contents: write
    secrets: inherit

  # dev work reaches protected master only through a PR merge.
  sync:
    name: Sync release branch
    needs: release
    if: always() && github.ref_name == 'dev' && !failure() && !cancelled()
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Merge dev into master via pull request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git fetch origin master dev
          if [ "$(git rev-list --count origin/master..origin/dev)" -eq 0 ]; then
            echo "master already contains dev."
            exit 0
          fi
          gh pr create --base master --head dev \
            --title "chore(release): merge dev into master" \
            --body "Automated release sync." || true
          gh pr merge --merge dev || { sleep 5; gh pr merge --merge dev; }

  deploy:
    needs: [release, sync]
    if: always() && !failure() && !cancelled()
    uses: ./.github/workflows/family-deploy.yml
    with:
      branch: master
    secrets: inherit
